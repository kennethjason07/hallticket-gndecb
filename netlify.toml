[build]
  command = "echo 'No build needed'"
  functions = "netlify/functions"
  publish = "public"

# SPA redirect - send all non-function routes to index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
  force = false
